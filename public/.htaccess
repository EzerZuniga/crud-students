<IfModule mod_rewrite.c>
    RewriteEngine On

    # Servir assets almacenados fuera del document root
    RewriteRule ^assets/(.*)$ ../assets/$1 [L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
